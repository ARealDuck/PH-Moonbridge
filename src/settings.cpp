#include "settings.hpp"

bool settings::check() {
    try {
        std::ifstream settingsload(filelocation);
        if (!settingsload.is_open()) {
           spdlog::
        }
    }
    return false;
}
